<p>组建项目团队完成后，系统在指定软件项目的根目录下，以运行时 CLI 名称命名生成隐藏工作目录。以 <b>Claude Code</b> 为例，会生成 <b>.claude/</b> 文件夹，其内部结构包含：agents/（已部署的 Agent 配置）、skills/（该项目用到的全部 Skill 包）、rules/（复制的编码规范）、knowledge/（领域知识库）。工程师只需在项目根目录执行 claude 命令，Claude Code 即自动加载 .claude/ 下的完整配置，AI 立即进入"已了解该项目所有领域知识与规范"的就绪状态，零上下文重建成本，开箱即用。</p>